name: Production — Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    name: Build & verify before release
    runs-on: ubuntu-latest
    timeout-minutes: 45

    # El repositorio tiene los permisos por defecto del GITHUB_TOKEN en solo lectura,
    # asi que sin esto el paso "Create GitHub Release" responde 403.
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Run unit tests
        working-directory: backend
        run: ./mvnw test -B

      - name: Build production image
        run: docker build -t inventory-backend:${{ github.ref_name }} ./backend

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          name: "Release ${{ github.ref_name }}"
